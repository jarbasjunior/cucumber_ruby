---
  default: -p html -p bdd
  bdd: --format pretty
  progress: --format progress
  html: --format html --out=log/report.html
  chrome: -p progress BROWSER=chrome
  chrome_headless: -p progress BROWSER=chrome_headless
  firefox: -p progress BROWSER=firefox
  firefox_headless: -p progress BROWSER=firefox_headless